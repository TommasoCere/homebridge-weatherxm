{
  "pluginAlias": "WeatherXM",
  "pluginType": "platform",
  "schema": {
    "title": "WeatherXM",
    "type": "object",
    "required": ["apiKey", "stationId"],
    "properties": {
      "apiKey": {
        "title": "WeatherXM API Key",
        "type": "string"
      },
      "stationId": {
        "title": "Station ID",
        "type": "string",
        "description": "L'ID della stazione WeatherXM da monitorare (obbligatorio)."
      },
      "apiCallsPerMonth": {
        "title": "Numero massimo chiamate API al mese",
        "type": "number",
        "default": 1000,
        "description": "Limite mensile di chiamate API (default 1000). L'intervallo di aggiornamento verrà calcolato automaticamente."
      },
      "logToFile": {
        "title": "Abilita log su file",
        "type": "boolean",
        "default": true
      },
      "debug": {
        "title": "Modalità debug",
        "type": "boolean",
        "default": false
      }
    }
  }
}
